export default function ARScreen() {
  return (
    <section className="screen">
      <h2>AR</h2>
      <p className="muted">
        Camera + AR asset recognition lands here in a later phase. This screen
        exists now so the shell, routing, and error isolation are real from day one.
      </p>
    </section>
  );
}
